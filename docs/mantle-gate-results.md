
# Mantle Gate Check — 2026-06-13T01:13:27.878Z

| Gate | HTTP | Pass | Note |
|------|------|------|------|
| A — 0x Mantle Sepolia (5003) | 401 | no | Auth required (endpoint reachable) |
| A — 0x Mantle mainnet (5000) | 401 | no | Auth required (endpoint reachable) |

**Locked decision:** PRIMARY_CHAIN=5000 (default; re-run with ZERO_EX_API_KEY to confirm quotes)

**Gate B (Etherspot):** not probed in CI — client uses injected EOA fallback per `docs/MANTLE_HACKATHON.md`.

**Gate C tokens:** Sepolia WMNT `0x19f5…deD5`; mainnet USDC/WMNT in `plugin-mantle-dex/src/config/tokens.ts`.

**Signing MVP:** server `MANTLE_PRIVATE_KEY` (demo funds only).


# Mantle Gate Check — 2026-06-13T01:40:47.953Z

| Gate | HTTP | Pass | Note |
|------|------|------|------|
| A — 0x Mantle Sepolia (5003) | 401 | no | Auth required (endpoint reachable) |
| A — 0x Mantle mainnet (5000) | 401 | no | Auth required (endpoint reachable) |

**Locked decision:** PRIMARY_CHAIN=5000 (default; re-run with ZERO_EX_API_KEY to confirm quotes)

**Gate B (Etherspot):** not probed in CI — client uses injected EOA fallback per `docs/MANTLE_HACKATHON.md`.

**Gate C tokens:** Sepolia WMNT `0x19f5…deD5`; mainnet USDC/WMNT in `plugin-mantle-dex/src/config/tokens.ts`.

**Signing MVP:** server `MANTLE_PRIVATE_KEY` (demo funds only).

